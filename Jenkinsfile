pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build Stage from GitHub Jenkinsfile'
            }
        }

        stage('Test') {
            steps {
                echo 'Test Stage from GitHub Jenkinsfile'
            }
        }

    }
}
